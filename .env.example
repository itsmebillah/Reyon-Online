# REYON Business OS environment-variable contract
#
# No application runtime or public environment variables are approved in Sprint 0.
# Add variable names here only when the architecture defines their owner, purpose,
# exposure (server-only or public), source, environments, and rotation requirements.
# Never place real credentials, tokens, project passwords, or secret values in this file.
